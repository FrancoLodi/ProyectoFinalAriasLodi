import React from 'react';

const NotFound = () => {
    return (
        <div style={{display:'flex', flexDirection:'column', alignItems:'center'}}>
            <p style={{fontSize:'90px', marginBottom:'0'}}>
                404
            </p>
            <p style={{fontSize:'20px'}}>
                Page not found.
            </p>
        </div>
    )
};

export default NotFound;